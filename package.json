{
  "name": "ecorecicla-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix src/backend && npm install --prefix src/frontend && npm run build --prefix src/frontend",
    "start": "node src/backend/server.js"
  }
}